{
  "name": "downstream-hub-api",
  "version": "1.0.0",
  "description": "Downstream Hub REST API — auth, app metadata CRUD, SSO token generation",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node scripts/run-migrate.js",
    "test": "node node_modules/jest/bin/jest.js --runInBand --forceExit"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.2.1",
    "helmet": "^8.3.0",
    "jose": "^5.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "nodemailer": "^8.0.5",
    "pg": "^8.11.3",
    "redis": "^4.6.12"
  }
}
