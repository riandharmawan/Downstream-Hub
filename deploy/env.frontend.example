# Downstream Hub — Frontend server (172.28.92.56)
# Copy to /opt/downstream-hub/frontend/.env (or set in CI/build)
# Used at BUILD TIME for Vite (VITE_API_URL).

# Backend API base URL (browser will call this). Use backend server IP and port.
VITE_API_URL=http://172.28.92.57:4000
