# Downstream Hub — Database server (172.28.92.60)
# Copy to /opt/downstream-hub/.env on the DB host

# ---------- PostgreSQL (Docker) ----------
POSTGRES_USER=hub
POSTGRES_PASSWORD=CHANGE_ME_PASSWORD
POSTGRES_DB=downstream_hub
